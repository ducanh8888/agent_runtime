# H7 — production cutover (performed; scale and sub-agent items outstanding)

Date: 2026-09-12 (Asia/Bangkok).
Source revision cut over: `a657c19`.

## Outcome

The production daemon now runs the source build rather than the pre-H0 snapshot
that had been installed since 2026-09-09. The state directory was backed up
first, the old daemon was stopped cleanly, the tool was reinstalled, and the new
daemon loaded all 44 retained sessions. Migration was rehearsed on a restored
copy of the backup, and a bounded live session wrote its file and was verified
on disk.

H7 is **not** complete: staged scale verification and the three sub-agent items
recorded in H7.9 remain, as does the client-side MCP reconnect below.

## What was done, with what was observed

1. **Inventory.** 44 sessions: 42 finished, 2 paused, **none running**; 21
   carry a `keep` tag. Both paused sessions are evidence (`cef7a889`, and
   `6ff256c9` which `docs/results/p3.md` names as not to be deleted).
2. **Backup.** `~/.agentrt-backups/pre-h7-20260912-002127/state.tar.gz`,
   26 MB, 10,437 entries, 44 `meta.json` records; the regenerable `cache/`
   directory is excluded.
3. **Daemon stopped.** `agentrt daemon stop` → the old process logged
   "Application shutdown complete" and "Finished server process [22806]".
4. **Reinstalled.** `uv tool install ./packages/agentrt-runtime --reinstall`.
5. **New build identified.** `agentrt --version` reports `agentrt-runtime
   0.1.0`; `inspect_tools.py` (H1) is present; `agentrt usage` (H6) exists; and
   the installed MCP surface is the full 15 tools —
   `artifacts, capacity, control, dispatch, dispatch_many, finalize, list,
   profiles, read_evidence, result, status, transcript, usage, wait_all,
   wait_any`.
6. **Daemon started on the real state.** Health 200; **44 sessions loaded**;
   `GET /capacity` returns `{limit: 10, running: 0, queued: 0, available: 10,
   in_flight_llm: 0}` and `GET /spend-archive` returns an empty ledger, so the
   H5 and H6 surfaces are live in production. The daemon took a new port
   (38887 → 40785) because the previous port is not reserved; clients read it
   from `daemon.json`.
7. **Migration and rollback rehearsed on a copy.** The backup was extracted
   into a temporary state directory and a daemon was started on it with the new
   build: 44 sessions loaded, 21 evidence tags intact, capacity available 10.
   That daemon was stopped and the copy removed. This is what demonstrates that
   the pre-cutover state loads under the new code and that the backup is
   restorable; it is not a rehearsal of running the *old* code again.
8. **Live smoke.** Session `e0eb6132` ("H7 cutover smoke", `max_iterations` 6,
   `keep=evidence`): status `finished`, `result_state: final`,
   `admission_status: admitted`, `iterations_used: 2`. Its answer said it had
   created the file, so the claim was checked on disk rather than believed:
   `OK.txt` exists and contains `ok`. `agentrt usage` reported the worker's
   model and `first_reasoning_token_latency_ms: 1483.669` with
   `first_token_latency_ms: null` — the H6 timing is live, and the null is the
   documented "not recorded" rather than a zero.

## Verification

Phase gates accumulated over H0–H6, all run before this cutover:

```text
server suite                                      2239 passed, 13 deselected (5:11)
runtime + sdk/conversation                        920 passed
tests/sdk/llm                                     1040 passed, 2 known baselines
tools/scope.py --phase h6                         1798 passed, 3 known failures
changed files under Pyright                       0 errors
```

The known failures are the two SDK baselines recorded in the H0 result and
`test_check_tool_installed_python`, which wants a `python` shim this machine
does not have.

## Remaining before H7 can be called complete

- **Staged scale verification.** No 50–100 worker run has been attempted, and
  the load, window and host-pressure thresholds have to be agreed before one is.
- **The three sub-agent gaps** recorded as H7.9 (context inheritance, a stall
  watchdog, progress visibility).
- **The client-side MCP reconnect is a user action.** The stdio MCP servers
  running inside existing orchestrators hold the old code in memory and may
  also hold the old daemon port from before the cutover; a restart of those
  orchestrators is what loads the new surface. This session's own MCP server is
  one of them, which is why the new tools are not callable from here.
- The H5 shared-workspace writer limit remains unimplemented.

## Rollback

The pre-cutover backup is
`/home/ducanh/.agentrt-backups/pre-h7-20260912-002127/state.tar.gz`. Rollback is
to stop the daemon, restore that archive over the state directory, and reinstall
the revision it was taken from. Do not run the old build against a state
directory that a later migration has mutated; the backup exists precisely so
that this is never necessary. Work created after the cutover must be reconciled,
not deleted — the backup does not contain it.
